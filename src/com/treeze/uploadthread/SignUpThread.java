package com.treeze.uploadthread;import java.io.BufferedReader;import java.io.IOException;import java.io.InputStream;import java.io.InputStreamReader;import java.nio.charset.Charset;import org.apache.http.HttpEntity;import org.apache.http.HttpResponse;import org.apache.http.client.HttpClient;import org.apache.http.client.methods.HttpPost;import org.apache.http.entity.mime.HttpMultipartMode;import org.apache.http.entity.mime.MultipartEntity;import org.apache.http.entity.mime.content.StringBody;import org.apache.http.impl.client.DefaultHttpClient;import org.apache.http.util.EntityUtils;import com.treeze.data.TreezeStaticData;import com.treeze.data.User;public class SignUpThread extends Thread{	public SignUpThread(String identificationNumber, String password, String email, String userNm,			String userType) {	  //		User user = u;			HttpClient httpClient = new DefaultHttpClient();		       HttpPost post = new HttpPost("http://" + TreezeStaticData.IP + ":8080/treeze/createUser"); 		       MultipartEntity multipart = new MultipartEntity(						HttpMultipartMode.BROWSER_COMPATIBLE, null,						Charset.forName("UTF-8"));  // xml, classId, 		       StringBody typeBoby;			try {				typeBoby = new StringBody(userType, Charset.forName("UTF-8"));				StringBody idBody = new StringBody(identificationNumber + "", Charset.forName("UTF-8"));				StringBody nameBody = new StringBody(userNm, Charset.forName("UTF-8"));				StringBody emailBody = new StringBody(email, Charset.forName("UTF-8"));				StringBody pwBody = new StringBody(password, Charset.forName("UTF-8"));				multipart.addPart("userType", typeBoby);				multipart.addPart("identificationNumber", idBody);				multipart.addPart("userName", nameBody);				multipart.addPart("userEmail", emailBody);				multipart.addPart("password", pwBody);				post.setEntity(multipart);				HttpResponse response = httpClient.execute(post);				HttpEntity resEntity = response.getEntity();				InputStream inputStream = resEntity.getContent();		    	  String str = "";		    	  String tmp;		    	  BufferedReader in = new BufferedReader(new InputStreamReader(inputStream, "UTF-8"));					byte[] b = new byte[4096];					while((tmp = in.readLine()) != null )						str += tmp;										System.out.println("[SignUp Response Result]" +str);				EntityUtils.consume(resEntity);				System.out.println("UploadtoServer : singUp()");			} catch (IOException e) {				// TODO Auto-generated catch block				e.printStackTrace();			}		  }		// TODO Auto-generated constructor stub	}